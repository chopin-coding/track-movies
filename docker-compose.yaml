
# Use root/example as user/password credentials
version: '3.1'

services:
  mongo:
    image: mongo:5.0.14
    restart: always
    ports:
      - "27017:27017"

    networks:
      - my-network

networks:
  my-network:


